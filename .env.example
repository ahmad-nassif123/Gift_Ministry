# عنوان الموقع الكامل (لرموز QR في PDF وغيرها عند الحاجة خارج المتصفح)
# مثال: https://gift-catalog.vercel.app
# NEXT_PUBLIC_SITE_URL=

# ——— الشعار (اختياري) ———
# مسار ملف الشعار تحت public/، يبدأ بـ /. إن تركت فارغاً يُستخدم المسار الافتراضي في lib/config.ts (حالياً /logo.png).
# مثال: وضعت الملف public/logo-org.png  ← اضبط: NEXT_PUBLIC_SITE_LOGO_PATH=/logo-org.png
# ثم أعد البناء (npm run build) حتى يُحدَّث الواجهة والـ manifest.
# NEXT_PUBLIC_SITE_LOGO_PATH=

# ——— شعار ملفات الـPDF (اختياري) ———
# افتراضيًا يستخدم المشروع /logo_pdf.png داخل public/.
# مثال: NEXT_PUBLIC_PDF_LOGO_PATH=/logo_pdf.png
# NEXT_PUBLIC_PDF_LOGO_PATH=

# أيقونة الدرع (تبويب المتصفح + أيقونات PWA). الافتراضي في الكود: /emblem.png — انسخ الملف إلى app/icon.png و app/apple-icon.png أيضاً.
# NEXT_PUBLIC_SITE_ICON_PATH=

# ——— أمان لوحة التحكم (مهم للإنتاج) ———
# ⚠️ لا تضع كلمات المرور الحقيقية في GitHub. استخدم Vercel → Environment Variables ثم Redeploy.
#
# ——— كلمتا مرور منفصلتان (Vercel → Environment Variables) ———
#
# 1) /login و /dashboard — لوحة التحكم:
ADMIN_PASSWORD=
#
# 3) /private — الهدايا الخاصة (صفحة منفصلة عن الكتالوج العام):
PRIVATE_CATALOG_PASSWORD=
#
# 2) /admin/pricing — أسعار الهدايا والفواتير (مطلوب في الإنتاج):
# ADMIN_PRICING_PASSWORD=
#
# إذا تركت ADMIN_PASSWORD فارغاً يُجرَّب ADMIN_LOGIN_PASSWORD كبديل مؤقت (للتوافق مع إعدادات قديمة).
#
# بريد ثابت يُخزَّن في الجلسة لتتبّع بعض عمليات API (سجلات المستودع وغيره) — لا يُعرَض في نموذج الدخول:
# DASHBOARD_SESSION_EMAIL=audit@your-org.example
#
# AUTH_SECRET: ضروري في الإنتاج. استخدم سلسلة عشوائية 32+ حرفاً (مثلاً: openssl rand -base64 32)
AUTH_SECRET=غيّر_هذا_إلى_سلسلة_عشوائية_طويلة_32_حرف_على_الأقل

# بديل إن كان ADMIN_PASSWORD فارغاً (اختياري)
# ADMIN_LOGIN_PASSWORD=

# Postgres مطلوب لصفحة «حسابات الدخول» وGift Scanner وغيرها، وليس لشرط الدخول الأساسي.
POSTGRES_URL=
# DATABASE_URL=

# ——— رفع صور الهدايا (Vercel Blob) ———
# BLOB_STORE_ID + BLOB_READ_WRITE_TOKEN (Restore Token من Settings)
# للمخزن الخاص gift-ministry-blob يُستخدم access=private تلقائياً.
# لمخزن Public فقط: BLOB_ACCESS=public
# BLOB_ACCESS=private

# ——— توقيع رقمي لملفات PDF (اختياري) ———
# مسار شهادة PKCS#12 (p12/pfx) على الخادم فقط (لا تضعها داخل الريبو)
PDF_SIGN_P12_PATH=
# كلمة مرور الشهادة إن وجدت
PDF_SIGN_P12_PASSPHRASE=

# البريد الإلكتروني (إرسال PDF - اختياري)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
ORDER_EMAIL_TO=

# ——— ملاحظة أمان ———
# في الإنتاج: AUTH_SECRET + ADMIN_PASSWORD + PRIVATE_CATALOG_PASSWORD + ADMIN_PRICING_PASSWORD؛ ثم POSTGRES_URL لميزات القاعدة.
# للتخفيف من هجمات التخمين على تسجيل الدخول يمكن لاحقاً إضافة Rate Limiting (مثلاً عبر Upstash).
